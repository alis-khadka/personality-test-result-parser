<%= render partial: "show_parsed_data", locals: { title: 'The result parsed from the provided data is shown below:', data: @parsed_result } %>

<%= button_to "Verify from RecruitBot API", { action: "verify_result" }, method: :get, class: "btn btn-secondary mt-3 ml-5" %>
